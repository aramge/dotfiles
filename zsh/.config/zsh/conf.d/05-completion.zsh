# Tab-Vervollständigung für .. aktivieren
zstyle ':completion:*' special-dirs ..

# Case-insensitive Autovervollständigung
zstyle ':completion:*' matcher-list 'm:{a-zA-Z}={A-Za-z}'

# Interaktives Menü für die Auswahl (mit Pfeiltasten navigieren)
zstyle ':completion:*' menu select
